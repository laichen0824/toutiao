<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nowcoder.dao.NewsDAO">
    <sql id="table">news</sql>
    <sql id="selectFields">id,title, link, image, like_count, comment_count,created_date,user_id
    </sql>
    <select id="selectByUserIdAndOffset" resultType="com.nowcoder.model.News">
        SELECT
        <include refid="selectFields"/>
        FROM
        <include refid="table"/>
        WHERE status = 1
        <if test="userId != 0">
            and user_id = #{userId}
        </if>
        ORDER BY created_date DESC
        LIMIT #{offset},#{limit}
    </select>

    <select id="getNewsList" resultType="com.nowcoder.model.News">
        SELECT
        news.id,title, link, image, like_count, comment_count,news.created_date,user_id,user.name author
        FROM
        <include refid="table"/>,user
        WHERE news.status = 1 and user.id = news.user_id
        <if test="name != null">
            and title like ${name}
        </if>
        ORDER BY created_date DESC
    </select>

    <select id="getCount" resultType="int">
        SELECT
        count(id)
        FROM
        <include refid="table"/>
        WHERE status = 1
        <if test="name != null">
            and title like ${name}
        </if>
    </select>
</mapper>
